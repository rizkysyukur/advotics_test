import { Layout } from 'antd';

const Heading = () => {
	const { Header } = Layout;

	return (
		<Header style={{ background: '#FFFFFF', boxShadow: '0px 3px 6px #00000029' }}>
			Header
		</Header>
	)
}

export default Heading;